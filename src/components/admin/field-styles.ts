/**
 * The admin form-control styles, in one place.
 *
 * This exact string was copy-pasted into six page files before the lead
 * tracker arrived; new code imports it rather than adding a seventh copy.
 */
export const inputClassName =
  "w-full rounded-2xl border border-slate-300 bg-white px-4 py-3 text-sm text-slate-900 outline-none transition placeholder:text-slate-400 focus:border-sky-700 focus:ring-4 focus:ring-sky-100";

/** The compact variant used inside dense table rows and filter bars. */
export const compactInputClassName =
  "w-full rounded-xl border border-slate-300 bg-white px-3 py-2 text-sm text-slate-900 outline-none transition placeholder:text-slate-400 focus:border-sky-700 focus:ring-4 focus:ring-sky-100";

/** The house pill button — bordered, neutral, used for row and card actions. */
export const pillButtonClassName =
  "inline-flex items-center rounded-full border border-slate-300 px-3 py-1.5 text-xs font-semibold text-slate-700 transition hover:border-slate-400 hover:bg-slate-50 disabled:cursor-not-allowed disabled:opacity-50";
